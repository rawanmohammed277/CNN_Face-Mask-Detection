# CNN_Face-Mask-Detection
Face Mask Detection using Convolutional Neural Networks (CNN). This project classifies images into two categories: with mask and without mask. It includes data preprocessing, image augmentation, CNN model development using TensorFlow/Keras, model evaluation, and visualization of training performance.
